---
title: "OSP-PBPK-Model-Library"
author: "Open System Pharmacology"
date: "`r Sys.Date()`"
output: github_document
---

<!-- README.md is generated from README.Rmd. Please edit that file -->

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
toolsData <- read.csv(
  "tools.csv",
  stringsAsFactors = FALSE,
  colClasses = "character"
)
modelsData <- read.csv(
  "models.csv",
  check.names = FALSE,
  stringsAsFactors = FALSE,
  colClasses = c("logical", rep("character", 5))
)
qualificationRunnerFolder <- "QualificationRunner/QualificationRunner"
pkSimPortableFolder <- "PK-Sim/PK-Sim"
source("report-qualification-template.R")
```

## PBPK Models

```{r, results='asis'}
knitr::kable(modelsData)
```

## Tools

```{r, results='asis'}
knitr::kable(toolsData)
```
